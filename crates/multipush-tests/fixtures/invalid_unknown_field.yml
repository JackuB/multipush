provider:
  type: github
  org: test-org
  token: ghp_test

policies:
  - name: bad-policy
    severity: error
    targets:
      repos: "test-org/*"
    rules:
      - !ensure_flie
        path: README.md
